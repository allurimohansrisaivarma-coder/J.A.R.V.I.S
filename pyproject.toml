[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jarvis-assistant"
version = "0.2.7"
description = "Jarvis — Desktop Intelligence Platform"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0.0",
    "groq>=0.11.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "pyyaml>=6.0",
    "structlog>=24.0.0",
    "sounddevice>=0.4.6",
    "soundfile>=0.12.1",
    "numpy>=1.26.0",
    "edge-tts>=6.1.12",
    "onnxruntime>=1.17.0",
    "pygame>=2.5.2",
    "httpx>=0.27.0",
    "lancedb>=0.6.0",
    "sentence-transformers>=3.0.0",
    "sqlalchemy>=2.0.0",
    "ddgs>=1.0.0",
    "beautifulsoup4>=4.12.0",
    "mss>=9.0.0",
    "Pillow>=10.0.0",
    "dxcam>=0.3.0",
    "pywin32>=306",
    "imagehash>=4.3.1",
    "pywebview>=5.0",
    "websockets>=14.0",
    "pynput>=1.7.6",
    "google-api-python-client>=2.0.0",
    "google-auth-oauthlib>=1.0.0",
    "mcp>=1.0.0,<2.0.0",
    "playwright>=1.62.0",
    "psutil>=7.2.2",
    "pypdf>=6.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
    "pyinstaller>=6.0.0",
]

[project.scripts]
jarvis = "jarvis.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/jarvis"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src"]

[tool.ruff.lint]
# External API, device, browser, and UI boundaries log and degrade gracefully.
ignore = ["BLE001"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v --cov=jarvis"
asyncio_default_fixture_loop_scope = "function"

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
ignore_missing_imports = true
follow_imports = "skip"
no_implicit_optional = true
warn_unused_configs = true
warn_unused_ignores = true
